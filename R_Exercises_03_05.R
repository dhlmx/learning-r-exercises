min(myVector, na.rm = FALSE)
max(myVector, na.rm = FALSE)

myVector <- c(2, 4, -1, 0.5, 11, 3, 2)
abs(diff(myVector))

myXtrems <- c(max(myVector, na.rm = FALSE), min(myVector, na.rm = FALSE))
abs(diff(myXtrems))


